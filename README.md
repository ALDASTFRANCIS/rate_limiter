Custom Rate Limiter

A thread-safe, extensible Rate Limiter implementation in Java. This project demonstrates Low-Level Design (LLD) principles, concurrency handling, and the Strategy pattern to manage API request limits for different user tiers.
